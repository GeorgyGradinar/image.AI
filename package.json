{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run build && git add dist && git commit -m \"adding dist subtree\" && git subtree push --prefix dist origin gh-pages "
  },
  "devDependencies": {
    "@types/node": "^18",
    "nuxt": "^3.5.0"
  },
  "dependencies": {
    "@globalhive/vuejs-tour": "^1.3.5",
    "@mdi/font": "^7.2.96",
    "@pinia/nuxt": "^0.4.11",
    "@vuelidate/core": "^2.0.2",
    "@vuelidate/validators": "^2.0.2",
    "pinia": "^2.1.3",
    "sass": "^1.51.0",
    "vuetify": "^3.0.1"
  },
  "overrides": {
    "vue": "latest"
  }
}
